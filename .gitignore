# ── Gradle ──────────────────────────────────────────────────────────────────
*.iml
.gradle
/local.properties
local.properties
/build
**/build/
/captures
.externalNativeBuild
.cxx


# ── IDE ─────────────────────────────────────────────────────────────────────
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
stack_trace.txt

# ── Kotlin Multiplatform build artifacts ────────────────────────────────────
.kotlin/

# ── iOS / Xcode ─────────────────────────────────────────────────────────────
iosApp/Pods/
xcuserdata/
**/*.xcuserstate
iosApp/*.xcworkspace/xcuserdata/

# ── Firebase config — never commit ──────────────────────────────────────────
google-services.json
app/google-services.json
GoogleService-Info.plist
**/GoogleService-Info.plist

# info.zip contains both Firebase config files and IS committed intentionally
!info.zip

# ── Local credential files — NEVER commit ────────────────────────────────────
cred.txt
*.cred.txt
secrets.txt
*.secrets

# ── Secrets & signing ────────────────────────────────────────────────────────
*.keystore
**/*.keystore
*.jks
**/*.jks
*.p12
**/*.p12
*.pfx
**/*.pfx
*.pem
**/*.pem
*.crt

# ── Build outputs ────────────────────────────────────────────────────────────
scripts.zip
*.apk
**/*.apk
*.aab
**/*.aab

scripts/